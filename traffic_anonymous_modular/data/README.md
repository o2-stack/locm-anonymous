# Optional Data

Place `Metro_Interstate_Traffic_Volume.csv` here or in the working directory if real traffic profiles are desired. If the file is missing, the environment automatically uses synthetic traffic demand profiles.
