# Reproducibility Notes

The traffic experiments are stochastic. Exact point-wise reproduction of every training curve is not expected across machines. If `Metro_Interstate_Traffic_Volume.csv` is unavailable, the environment falls back to synthetic demand profiles.
