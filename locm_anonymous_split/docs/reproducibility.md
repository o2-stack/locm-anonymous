# Reproducibility

The main training entry point is `python -m src.train`. Results are written to `results/<run_id>/`.

The code reports stochastic MARL training behavior. Different hardware, dependency versions, and solver convergence behavior can lead to different point-wise learning curves.
